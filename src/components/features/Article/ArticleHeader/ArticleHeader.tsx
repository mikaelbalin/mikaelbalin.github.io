import { Container, Title } from "@mantine/core";

export const ArticleHeader = () => {
  return (
    <Container className="mt-16 pt-6">
      <Title size="h2">
        Decide to render a partial or not dynamically in Astro
      </Title>
    </Container>
  );
};
